<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    {% if dev %}
        <script type="module" src="/src/main.ts"></script>
        <link rel="stylesheet" href="/src/style.css">
        <script src="http://localhost:35729/livereload.js?snipver=1"></script>
    {% else %}
        <script type="module" src="/assets/{{ jsFile }}"></script>
        <link rel="stylesheet" href="/assets/{{ cssFile }}">
    {% endif %}
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-8">
        <h1 class="text-3xl font-bold mb-8 text-gray-800">Product Data Test</h1>
        
        <div class="bg-white p-6 rounded-lg shadow-lg border border-gray-200">
            <h2 class="text-xl font-semibold mb-4 text-gray-700">Product Information:</h2>
            
            <div class="space-y-2">
                <p class="text-gray-600"><strong class="text-gray-800">Name:</strong> {{ product.name }}</p>
                <p class="text-gray-600"><strong class="text-gray-800">Price:</strong> ${{ product.price }}</p>
                <p class="text-gray-600"><strong class="text-gray-800">Description:</strong> {{ product.description }}</p>
            </div>
        </div>
        <div x-data="toggle" class="mt-8">
            <button @click="toggle" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded transition-colors">Show/Hide</button>
            <div x-show="open" class="mt-2 p-2 bg-green-100 text-green-800 rounded border border-green-200">Alpine.js test 🎉</div>
        </div>
    </div>
</body>
</html>